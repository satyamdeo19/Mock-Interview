AI Mock Interview System

A complete multimodal AI interview evaluation platform that analyzes text, audio, and video to produce behavioral scores, SHAP explainability, and personalized feedback.

The system extracts 120+ multimodal features, performs feature engineering, fuses modalities, uses trained ML models, and generates structured feedback with visualizations.

Project Structure
